<!DOCTYPE html>
<html lang="en">
  <head>
    {% block head %}
      <title>{{title}}</title>
      <meta name="description" content="{{title}}" />
    {% endblock %}
    {% include "head.njk" %}
  </head>
  <body>
    <div class="container">{{ content | safe }}</div>
  </body>
</html>
